export interface Encuestas {
  id: number;
  encuestador: string;
  cod_area: number;
  fecha: Date;
  n_listado: number;
  n_vivienda: number;
  n_hogar: number;
  accion: string;
}

/* DATE.JS (MOMENT.JS) TUTORIAL */
